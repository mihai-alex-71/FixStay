services:
  postgres:
    image: postgres:16
    container_name: fixstay-db
    environment:
      POSTGRES_DB: fixstay
      POSTGRES_USER: fixuser
      POSTGRES_PASSWORD: 123456
    ports:
      - "5432:5432"
    volumes:
      - fixstay_data:/var/lib/postgresql/data

volumes:
  fixstay_data: